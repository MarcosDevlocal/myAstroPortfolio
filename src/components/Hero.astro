---
import { siteConfig } from "../config";
import SplitText from "./ui/SplitText";
import BlurText from "./ui/BlurText";
import MagneticButton from "./ui/MagneticButton";
import CountUp from "./ui/CountUp";
import TiltCard from "./ui/TiltCard";
---

<section
  id="hero"
  class="relative isolate flex min-h-[100dvh] flex-col justify-center overflow-hidden px-4 pb-24 pt-28 sm:px-6 sm:pt-32"
>


  <div class="mx-auto flex w-full max-w-6xl flex-col gap-14 lg:flex-row lg:items-center lg:gap-16">

    {/* ── LEFT — Hero text ──────────────────────────────────────────────── */}
    <div class="flex-1">
      {/* Availability pill */}
      <div class="mb-6 inline-flex items-center gap-2 rounded-full border border-[var(--glass-border)] bg-[var(--glass)] px-4 py-1.5 backdrop-blur-xl shadow-sm">
        <span class="relative flex h-2 w-2">
          <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-emerald-400 opacity-75"></span>
          <span class="relative inline-flex h-2 w-2 rounded-full bg-emerald-400"></span>
        </span>
        <span class="font-mono-label text-[var(--text-muted)]">Available for work · Warsaw</span>
      </div>

      {/* Eyebrow label */}
      <p class="font-mono-label mb-5 text-[var(--text-muted)]">
        Portfolio · Burgos → Warsaw → the world
      </p>

      {/* Animated name — SplitText per-character spring */}
      <SplitText
        client:load
        text={siteConfig.name}
        as="h1"
        className="text-pretty font-black text-[var(--text)] tracking-tighter"
        style={`font-size: clamp(4rem, 12vw, 8rem); line-height: 0.95; margin-left: -0.05em;`}
      />

      {/* Animated subtitle — BlurText per-word */}
      <BlurText
        client:load
        text={`${siteConfig.title} — ${siteConfig.description}`}
        as="p"
        className="mt-5 max-w-2xl text-pretty text-lg leading-[1.75] text-[var(--text-muted)] sm:text-xl"
        delay={0.5}
      />

      {/* CTA buttons */}
      <div class="mt-9 flex flex-wrap items-center gap-3">
        <MagneticButton client:load href="/projects" variant="primary" id="hero-cta-primary">
          View projects
          <svg class="ml-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 17L17 7M17 7H7M17 7V17"/>
          </svg>
        </MagneticButton>

        <MagneticButton client:load href={`mailto:${siteConfig.social.email}`} variant="outline" id="hero-cta-contact">
          Get in touch
        </MagneticButton>

        <MagneticButton client:load href="#konari" variant="ghost" id="hero-cta-konari">
          Konari nonprofit →
        </MagneticButton>
      </div>

      {/* Social links */}
      <div class="mt-8 flex items-center gap-5">
        {siteConfig.social?.email && (
          <a
            href={`mailto:${siteConfig.social.email}`}
            aria-label="Email"
            data-interactive
            class="text-[var(--text-muted)] transition-all duration-200 hover:text-[var(--indigo)] hover:scale-110"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M3 7a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7z"/><path d="M3 7l9 6 9-6"/>
            </svg>
          </a>
        )}
        {siteConfig.social?.linkedin && (
          <a
            href={siteConfig.social.linkedin}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="LinkedIn"
            data-interactive
            class="text-[var(--text-muted)] transition-all duration-200 hover:text-[var(--indigo)] hover:scale-110"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M8 11v5M8 8v.01M12 16v-5M16 16v-3a2 2 0 1 0-4 0M3 7a4 4 0 0 1 4-4h10a4 4 0 0 1 4 4v10a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4z"/>
            </svg>
          </a>
        )}
        {siteConfig.social?.github && (
          <a
            href={siteConfig.social.github}
            target="_blank"
            rel="noopener noreferrer"
            aria-label="GitHub"
            data-interactive
            class="text-[var(--text-muted)] transition-all duration-200 hover:text-[var(--indigo)] hover:scale-110"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M9 19c-4.3 1.4-4.3-2.5-6-3m12 5v-3.5c0-1 .1-1.4-.5-2 2.8-.3 5.5-1.4 5.5-6a4.6 4.6 0 0 0-1.3-3.2 4.2 4.2 0 0 0-.1-3.2s-1.1-.3-3.5 1.3a12.3 12.3 0 0 0-6.2 0C6.5 2.8 5.4 3.1 5.4 3.1a4.2 4.2 0 0 0-.1 3.2A4.6 4.6 0 0 0 4 9.5c0 4.6 2.7 5.7 5.5 6-.6.6-.6 1.2-.5 2V21"/>
            </svg>
          </a>
        )}
      </div>
    </div>

    {/* ── RIGHT — Visual abstract / Image ──────────────────────────────────── */}
    <div class="relative flex w-full shrink-0 flex-col items-center justify-center lg:w-[450px]">
      <TiltCard client:load maxTilt={10} glowColor="rgba(99,102,241,0.25)">
        <div class="relative w-full aspect-[4/5] overflow-hidden rounded-[2rem] border border-[var(--glass-border)] bg-[var(--glass)] shadow-2xl shadow-black/20 backdrop-blur-md">
          {/* Subtle inner glow */}
          <div class="absolute inset-0 z-10 shadow-[inset_0_0_40px_rgba(255,255,255,0.05)] rounded-[2rem] pointer-events-none"></div>
          
          <img 
            src="/images/hero-shape.png" 
            alt="Abstract design" 
            class="absolute inset-0 h-full w-full object-cover transition-transform duration-700 ease-out hover:scale-105"
          />
          
          {/* Floating glass badge */}
          <div class="absolute bottom-6 left-6 right-6 z-20 flex items-center justify-between rounded-2xl border border-[var(--glass-border)] bg-black/40 backdrop-blur-xl px-5 py-3 shadow-lg">
            <div class="flex flex-col">
              <span class="text-xs font-medium text-[var(--text-muted)] uppercase tracking-wider">Experience</span>
              <span class="text-xl font-bold text-white"><CountUp client:visible target={3} suffix="+" /> Years</span>
            </div>
            <div class="h-8 w-px bg-[var(--glass-border)]"></div>
            <div class="flex flex-col text-right">
              <span class="text-xs font-medium text-[var(--text-muted)] uppercase tracking-wider">Projects</span>
              <span class="text-xl font-bold text-white"><CountUp client:visible target={12} suffix="+" /> Shipped</span>
            </div>
          </div>
        </div>
      </TiltCard>
    </div>
  </div>

  {/* ── Scroll indicator ─────────────────────────────────────────────────── */}
  <div
    class="scroll-chevron absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-1.5"
    aria-hidden="true"
  >
    <span class="font-mono-label text-[var(--text-muted)] opacity-60">scroll</span>
    <svg
      width="16"
      height="16"
      viewBox="0 0 24 24"
      fill="none"
      stroke="var(--text-muted)"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <path d="M6 9l6 6 6-6"/>
    </svg>
  </div>
</section>

<style>
  @keyframes terminal-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }
  .terminal-cursor {
    animation: terminal-blink 1.1s step-end infinite;
  }
  @keyframes ping {
    75%, 100% { transform: scale(2); opacity: 0; }
  }
  .animate-ping {
    animation: ping 1.2s cubic-bezier(0, 0, 0.2, 1) infinite;
  }
</style>
